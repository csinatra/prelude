# Decision log

Dated index of key project decisions, with pointers to where each one is
implemented or argued in full. The audit trail for the writeup.

| Date | Decision | Recorded in |
|---|---|---|
| pre-2026-07-15 | No LLM calls inside the MLE-bench container (research-integrity invariant) | CLAUDE.md core constraint 1 |
| pre-2026-07-15 | Leave-one-out retrieval enforced at the retriever seam | CLAUDE.md core constraint 5; RESEARCH_DESIGN.md corpus section |
| pre-2026-07-15 | Judge rubric frozen before any evaluation run | docs/JUDGE_RUBRIC.md; CLAUDE.md core constraint 7 |
| 2026-07-15 | Notebook summaries generated by pinned Haiku, `summary_model`-stamped (corpus homogeneity) | RESEARCH_DESIGN.md corpus section; ingest/ingest_summaries.py |
| 2026-07-15 | Additive spec composition — each grid step changes exactly one thing (B1=block; B2/C1=+advice; C2=+structured spec) | harness/renderer.py docstring; RESEARCH_DESIGN.md workflow diagram |
| 2026-07-15 | `SIMILARITY_THRESHOLD=None` for v1 — global cutoffs break knowledge parity; top-k rank ordering is quality control | RESEARCH_DESIGN.md threats-to-validity (budget/attention confounds) |
| 2026-07-15 | Kaggle-measured effects interpreted as a conservative lower bound for ambiguous real-world settings | RESEARCH_DESIGN.md threats-to-validity (construct validity) |
| 2026-07-15 | LangSmith plan not upgraded — traces dark until ~Aug 1; results/ artifacts are the durable record | analysis/artifacts.py docstring; commit 172c925 |
| 2026-07-15 | Code4ML corpus expansion via Anthropic Batch API (~$200, same pinned summary model) | docs/COST_ESTIMATE.md |
| 2026-07-16 | Condition A demoted to contingent matched-agent arm with pre-registered trigger; published baseline citation-only | RESEARCH_DESIGN.md H1 amendment + Condition A design note |
| 2026-07-16 | Cloud box provisioned by script + persistent data volume, no custom container image for prelude | scripts/setup_cloudbox.sh header; docs/RUNBOOK.md |
| 2026-07-17 | AIDE agent model: single model pinned identically across the whole grid; exact version finalized on-box (aideml v6.3.3 support) | cloudbox/agents/aide-prelude/config.yaml; docs/RUNBOOK.md step 4 |
| 2026-07-17 | Two-sided cost/efficiency ledger (spec-side per-call usage; agent-side wallclock/steps/trajectories) | RESEARCH_DESIGN.md outcome metrics |
| 2026-07-19 | Spec-pipeline eval model pinned: `EVAL_MODEL=claude-sonnet-5` | pipeline/config.py; RESEARCH_DESIGN.md design notes |
| 2026-07-19 | Rubric SHA-256 pin test deferred until immediately before eval runs (pre-run amendments still expected) | CLAUDE.md core constraint 7 |
| 2026-07-22 | POC scope: eval on a small (3–5) subset of Lite-22; mechanistic spec-judging is the primary signal, MLE-bench score a conservative lower bound | RESEARCH_DESIGN.md Roadmap v1 + Outcome metrics |
| 2026-07-22 | `random-acts-of-pizza` reserved as the off-eval integration/smoke competition (excluded from the eval subset) | docs/RUNBOOK.md step 4 |
| 2026-07-22 | Smoke run decoupled from the matched-A anchor (8-step dev budget is not a valid A run); matched-A stays contingent, full-budget, on eval competitions | RESEARCH_DESIGN.md Condition A note; docs/RUNBOOK.md step 4 |
| 2026-07-22 | Batch driver queues by competition (all conditions of a problem contiguous) so per-problem analysis needn't wait on the full grid | harness/batch.py::pending_runs |
| 2026-07-22 | AIDE agent model resolved: `claude-haiku-4-5-20251001`, aideml v6.3.3 pristine (only an httpx<0.28 env pin). The 5-family/Opus-4.7+ removed `temperature` (400) and Sonnet 5 defaults thinking ON (multi-block response breaks aideml's single-text assert); Haiku 4.5 runs aideml unmodified | cloudbox/agents/aide-prelude/config.yaml + requirements.txt; RESEARCH_DESIGN.md |
| 2026-07-22 | Two-model roles: spec pipeline on Sonnet 5, AIDE agent on Haiku 4.5 — a deliberate advisor/executor split, both held constant across A/B/C so no B-vs-C confound; agent capability sets the operating point for the secondary (downstream) signal only. Sonnet-5-thinking-off adapter reserved as a future capability-sweep lever | RESEARCH_DESIGN.md threats-to-validity + roadmap |
| 2026-07-22 | Cloud-box bring-up chain encoded in setup_cloudbox.sh: Python 3.11 (Lambda ships 3.10), Sysbox runtime, base+agent image builds, agent dir copied not symlinked (glob discovery), Kaggle legacy-token requirement, base heavy-deps off by default | scripts/setup_cloudbox.sh; docs/RUNBOOK.md |
| 2026-07-23 | Implement aideml's Anthropic function-calling backend (upstream `NotImplementedError`) via tool use, so the AIDE feedback step runs single-provider on Claude — parity with its OpenAI backend, no algorithm change | cloudbox/agents/aide-prelude/patches/backend_anthropic.py + Dockerfile |
| 2026-07-23 | `git lfs pull` required in provisioning — mle-bench leaderboards are LFS objects; without it, grading medal-ranking fails on every competition | scripts/setup_cloudbox.sh |
| 2026-07-23 | B/C spec injection via a PRELUDE_SPEC_PATH hook: setup_cloudbox.sh patches mle-bench's agents/run.py to mount the env-named spec.md read-only at /home/spec/spec.md (run_agent has no --extra-mount; a --container-config volumes key collides with the built-in mount). Orchestration-only, held constant across A/B/C; applied as a committed .patch via `patch --forward --fuzz`. Wired + unit-tested; end-to-end B/C verification pending credits | cloudbox/agents/aide-prelude/patches/mlebench-run-spec-mount.patch; scripts/setup_cloudbox.sh; harness/batch.py::_run_agent |
| 2026-07-24 | Pre-eval data-collection audit → two fixes. (1) Batch driver now preserves agent-side artifacts (submission, journal, final solution code, token log) onto the persistent volume via `artifacts.preserve_agent_outputs` — without it, --terminate-on-done destroyed the mechanistic-judge inputs (H2), keeping only registry outcome fields (H1). (2) aide-prelude Anthropic backend logs per-call token usage to a behavior-neutral side-channel (`prelude_token_usage.jsonl`) — the deferred agent-side cost ledger, which can't be backfilled after runs. Motto: preserve more than the hypotheses strictly need | harness/batch.py; analysis/artifacts.py; cloudbox/agents/aide-prelude/patches/backend_anthropic.py; RESEARCH_DESIGN.md outcome metrics |
| 2026-07-24 | Spec pipeline → adopt ALL-CLOUD (direction chosen; implementation deferred). Spec-gen runs on the cloud-box HOST (outside the MLE-bench container, so core constraint 1 holds — the invariant is the container boundary, not the machine), then AIDE + grade: one machine, one registry. Replaces the laptop/cloud spec-vs-agent split, which becomes dev-vs-production (laptop = pipeline dev on the dev corpus). Rationale: kills the rsync/merge friction for batch runs, co-locates the corpus at full-Code4ML scale. Deferred to the corpus-scaling milestone (ingest full corpus onto the volume + re-calibrate in one pass). Delta: batch runner gains a registered→spec_built phase; box gets VOYAGE_API_KEY + CHROMA_PATH on the volume + a prelude venv; setup_cloudbox.sh additions; keep an explicit assert that spec-gen never runs in-container | CLAUDE.md core constraint 1; harness/batch.py; scripts/setup_cloudbox.sh |
| 2026-08-03 | Pipeline + injected-spec framing generalized from "competition" to "problem" (parse/surface/flag/advise prompts, RETRIEVAL_STANCE, B2/C1 freeform system, renderer CONTEXT_HEADER). MLE-bench/Kaggle is the measurement substrate, not the objective; the target capability is general ML problem framing. Metric-optimization language retained deliberately (the generalizable lever that still surfaces ensembling on merit — not medal-chasing), and the raw MLE-bench description is left verbatim as the honest input. Applied SYMMETRICALLY across all conditions, so it changes a constant applied equally and leaves the B-vs-C relative attribution untouched. Absolute medal rate may run slightly below a competition-tuned prompt; acceptable because the claim is the between-condition delta and MLE-bench score is already a conservative lower-bound secondary signal (2026-07-15, 2026-07-22). Watch floor effects: keep modeling advice strong/specific so conditions don't compress toward zero medals and lose power | pipeline/nodes.py; pipeline/condition_b.py; pipeline/condition_c1.py; harness/renderer.py |
| 2026-08-03 | Notebook-summary prompt upgraded ahead of the corpus scale-up: whole-notebook input (MAX_NOTEBOOK_CHARS 12k→60k; 12k fully covered only ~79% of Code4ML, and modeling/validation cells cluster at the notebook END, so the old front-biased cap starved abstracts of their most transferable content — 60k covers ~99% while still capping multi-hundred-KB outliers), and a detailed transfer-oriented abstract (specific estimators/hyperparameters, named feature transforms, validation scheme; was a 3–5 sentence sketch; max_tokens 512→1024). Richer output (not the cap) drives a Batch re-cost: ~$200 → ~$250–300, to update in COST_ESTIMATE before ingest. Existing dev summaries are now stale (regenerate on next ingest) | ingest/ingest_summaries.py; docs/COST_ESTIMATE.md |
| 2026-08-03 | Retrieval unit → notebook SUMMARY only; two-level code-chunk retrieval collapsed to a flat retrieve over notebook_summaries. Probe (analysis/probe_representation.py; 6 notebooks across NLP/tabular/vision) showed the rich whole-notebook summary already carries the transferable content (exact hyperparameters, feature recipes, validation schemes, distinctive techniques); curated code cells added mostly verbatim implementation + large lookup tables at 2–10× the summary's tokens, and by leave-one-out that verbatim code is always from a DIFFERENT competition — the least transferable part. Resolves four issues at once: code-cell signal density (boilerplate noise), the NL-problem↔code retrieval modality gap, cleaner B/C parity, system complexity; also production-faithful (summary/abstract-indexed retrieval is the standard RAG-over-code pattern). **Parity redefined to DISTINCT documents reasoned over**: B draws BASELINE_N_NOTEBOOKS in one flat pass; C's 3 directed stages retrieve independently (duplicates RETAINED — each stage reasons over its own top-N, no reasoning-time dedup, so a notebook relevant to multiple stages is used by each) with cross-stage top-up (retrieve_with_topup: each repeat backfilled by the next-best unseen doc) so every stage contributes STAGE_N distinct → 3×STAGE_N = BASELINE_N distinct. Token asymmetry from retained repeats is logged, not equalized. practitioner_knowledge stays ingested but unqueried at spec time (door open to curated-cell grounding later). The single-upfront-pool alternative was rejected: it tests directed *synthesis* over flat retrieval — a different question than directed *retrieval*, which is H1's mechanism. Caveats: N=6/Haiku; summary is a lossy interpretation vs verbatim ground truth (acceptable under "retrieval informs, never bounds") | analysis/probe_representation.py; notebooks/probe_summary_vs_cells.md; pipeline/retriever.py; pipeline/nodes.py; pipeline/condition_b.py; pipeline/condition_c1.py; pipeline/config.py; CLAUDE.md constraint 8; RESEARCH_DESIGN.md |
| 2026-08-03 | Embedding model: `voyage-code-3` → `voyage-4-large`. Once the retrieval unit became the notebook summary, retrieval is entirely NL↔NL (summary prose, competition descriptions, NL queries — no code embedded), so a general-purpose model fits better than a code-specialized one. voyage-4-large is SOTA general-purpose (32K ctx, 1024 default dims — matches the prior store) and *dominates* voyage-3-large: cheaper ($0.12 vs $0.18/M) AND a 200M free grant (voyage-3.x no longer free), so no cost crossover at any volume. Stays ~$0 at our scale (dev re-embed a few M tokens; full-corpus summaries ~55–110M, inside 200M free). The voyage-4 family (lite/4/large) share one embedding space → query-side can drop to a cheaper tier without re-embedding if volume ever exceeds free. `voyage-context-3/4` (contextualized-chunk models) considered and RESERVED: their contextualization helps *chunked* docs — wasted on atomic summaries, plus a different grouped-input API — revisit only if chunk-level curated-cell grounding returns. The swap requires re-embedding `notebook_summaries` (also new prompt) and `competition_metadata` (drop + rebuild via `--rebuild`) to keep query/doc space homogeneous. **`practitioner_knowledge` is DROPPED** (supersedes the retrieval-unit entry's "stays ingested" note): its voyage-code-3 vectors are now an incompatible-space liability and it's unqueried — reviving curated-cell grounding would re-embed from scratch anyway, so keeping stale data has no value. The ingest_notebooks module + config constant remain as a dormant rebuild mechanism. Coupled to the re-ingest, so it lands on the reingest branch, not the tested retrieval-logic branch | pipeline/embeddings.py; ingest/store.py (drop_collection); ingest/ingest_metadata.py + ingest_summaries.py (--rebuild); docs/COST_ESTIMATE.md |
| 2026-08-07 | Synthetic-corruption arm considered and REJECTED. Corrupting a benchmark input can only manufacture epistemic ambiguity, and MLE-bench's grading mechanism cannot represent the constitutive ambiguity the project motivation targets, so the arm would not have bought the construct-validity improvement it was proposed for | RESEARCH_DESIGN.md threats → construct validity |
| 2026-08-07 | Statistical analysis plan pre-registered: paired per-competition deltas, 90% percentile bootstrap, sign-test direction summary, and a three-part separation criterion for H1 | RESEARCH_DESIGN.md outcome metrics → analysis plan; analysis/stats.py |
| 2026-08-07 | C1 pilot reframed as a qualitative decomposition aid, not an isolation result; the powered v1 contrast is C2 vs B2 | RESEARCH_DESIGN.md experimental design → C1 pilot note |
| 2026-08-07 | Human-anchored judge validation added: blinded stratified sample of judged flags labeled against the frozen rubric, reported as percent agreement + Cohen's kappa with pre-committed interpretation bands, run BEFORE the mechanistic writeup. Judge model + rubric/prompt SHA-256 now recorded per run | docs/JUDGE_VALIDATION.md; analysis/judge_agreement.py; analysis/judge.py::judge_provenance |
| 2026-08-11 | Spec generation stays LOCAL, reversing the 2026-07-24 all-cloud decision. The handoff is small text files (spec.md plus append-only registry rows), so the rsync friction that motivated the move is minor, and that sync step doubles as the treatment-freeze point: specs are generated and inspected in one batch, committed, then run, which is the discipline the experiment wants anyway. Keeping spec-gen local also leaves LangSmith tracing on the machine holding the quota, avoids GPU idle during spec builds, and removes the need to host the corpus on the cloud volume. All-cloud's remaining benefit was one-machine operational simplicity for hands-off drains, which is modest at POC scale. Revisit if the corpus grows enough that shipping it to the box is cheaper than generating specs locally | harness/runner.py; harness/batch.py; docs/RUNBOOK.md two-machine split |
| 2026-08-11 | `SIMILARITY_THRESHOLD` now resolved from config at call time via a sentinel default, instead of being bound as a default argument at import. The old binding froze whichever value existed when the retriever was first imported, so an env change or a test monkeypatching config could leave the two disagreeing. Latent while the value is None; the pending recalibration is expected to set it | pipeline/retriever.py |
| 2026-08-11 | Negative-control spec arm (plausible but irrelevant spec, format and length held constant) deferred to v1.5 with the other mechanistic-decomposition arms: stage ablations and a stance ablation. v1 answers whether C2 separates from B2; these answer why, and each costs another condition across the eval subset | RESEARCH_DESIGN.md roadmap → deeper mechanistic evaluation |
| 2026-08-11 | Retrieval characterized on the rebuilt corpus (CORPUS-CONDITIONAL, re-run after expansion). Retrieval matches on TASK STRUCTURE across unrelated subject matter (horror-fiction authorship pulls toxic-comment moderation; materials science pulls taxi-fare regression), answering the topical-vs-problem-similarity risk. Condition C's staged retrieval beats B's flat pass at equal document budget on three axes: more distinct techniques (3/3 competitions), broader source competitions where coverage allows, and a lower share of low-substance notebooks (9.7% vs 15.3% against a ~35% corpus base rate). This shows C retrieves DIFFERENTLY, not that it retrieves better; usefulness is what the eval tests | analysis/retrieval_diversity.py; results/retrieval_diversity.json; results/retrieval_audit.md |
| 2026-08-11 | Diversity reranking (MMR) considered and REJECTED for v1. Retrieved sets are more self-similar than random, but that is what similarity ranking does rather than a pathology, and the initial reading of it as a defect was an analysis error corrected in review. Since retrieval breadth is part of what staged retrieval buys, applying MMR to both conditions would hand B breadth it does not otherwise have and mask the mechanism under test. Roadmapped with the v1.5 mechanistic arms | RESEARCH_DESIGN.md roadmap |
| 2026-08-11 | `SIMILARITY_THRESHOLD` stays `None` for v1, now on evidence rather than inference: per-competition medians differ widely (0.508 vs 0.436 across audited competitions), so any global cutoff would retain everything for one competition and drop everything for another. Top-k is already the per-competition relative rule | RESEARCH_DESIGN.md threats → budget/attention confounds |
| 2026-08-11 | Flag groundedness demoted from quality metric to descriptive, read asymmetrically: a present citation is verifiable evidence retrieval informed the flag; an absent one is uninformative, since the prompt sanctions honest general knowledge. Assumption flags are largely problem-structural and notebooks describe solutions rather than problem critiques, so a low grounded fraction is expected and must not be read as retrieval failure | RESEARCH_DESIGN.md mechanistic evaluation |
| 2026-08-11 | Corpus expansion decided: score-filtered Code4ML (non-zero `kaggle_score`) before the POC eval runs. ~25,939 notebooks over 583 competitions, 172 with >=20 notebooks, ~$72 against ~$300 unfiltered. 18 to 172 deep competitions addresses the measured concentration; 172 to 500 is diminishing returns since retrieval draws 24 documents. NO substance filter: corpus noise is production-representative AND is where C's advantage over B becomes measurable, so removing it would erase signal. Sequencing rationale: v1.5 (full Lite-22) reruns everything regardless, so optimizing v1's corpus for permanence buys nothing | docs/COST_ESTIMATE.md |
| 2026-08-11 | MLEModernizer evaluated as a corpus source. Same content universe (mined from Meta Kaggle / Meta Kaggle Code), but 79 competitions against Code4ML's ~1,150 and a 107 GB download, so it would worsen the coverage limitation it was considered to fix. Its distinguishing contribution, quality filtering of executed non-zero-score notebooks, is approximated free by the Code4ML score filter. Task-type stratification also investigated and found infeasible: 93% of Code4ML notebooks belong to competitions carrying no `datatype` label | zenodo 15022707; arXiv 2602.07195; docs/COST_ESTIMATE.md |
| 2026-08-11 | Low-substance retrievals characterized: they are largely Code4ML EXTRACTION ARTIFACTS (single-cell fragments) rather than shallow practitioner work, and their summaries honestly describe them as contentless. Injected low-quality context therefore costs B dilution of its document budget rather than misdirection, since nothing uncritically-adoptable is present. Shallow-near-neighbor competitions surface more fragments and are where the C2 retrieval stance should matter most | analysis/retrieval_diversity.py |
| 2026-08-11 | H2 narrowed to a within-C2 claim: flags are acted on at category-varying rates, rather than mediating the C2-vs-B2 difference. Flags exist only in C2, so the frozen rubric yields no comparable measure for B2's freeform advice. Recorded as an INSTRUMENT limitation that persists at any scale, distinct from sample-size limitations; a B-side judging instrument is roadmapped | RESEARCH_DESIGN.md H2 + threats |
| 2026-08-11 | Threats to validity now labels each limitation structural or POC-scale, and v1's purpose is stated explicitly: it does not claim representative outcomes, it claims the experiment functions as intended and produces the data needed to evaluate the research questions. Secondary metrics designated exploratory at POC scale on multiplicity grounds, while retaining their intended confirmatory-support role at design scale | RESEARCH_DESIGN.md "What v1 establishes" + outcome metrics |
| 2026-08-13 | H3 (efficiency) added pre-run: front-loading specification directs the agent's search, so conditioned runs converge faster. A claim about the path where H1 is about the endpoint, testing directed vs undirected iteration rather than upfront reasoning vs iterative feedback. Scale-free statistic, paired within competition: steps for the conditioned run to reach the baseline's final validation score, over baseline total steps. Steps lead over wall-clock; cost is reported but not the headline, being environment-specific. Scoped to convergence on VALIDATION, since the journal metric is the agent's own and only final submissions are graded; the validation-to-test relationship is checked and reported either way. Formalizes a question the 2026-07-17 ledger already collected data for, so the additive part is pre-registration. Depends on a step budget generous enough for trajectories to diverge | RESEARCH_DESIGN.md H3 + outcome metrics |
| 2026-08-17 | Notebook-summary prompt admits problem framing and practitioner rationale, reversing the approach-only scope. ADOPTED PENDING VALIDATION — the 50-notebook batch gates it before any full-corpus spend. Two separable things were stripped by the 2026-08-03 prompt: TOPICAL framing (branding, leaderboard, competition) and PROBLEM-STRUCTURAL insight (what the data constrained, where naive approaches misled, why a validation scheme was chosen). Stripping the first is validated and retained; stripping the second was accidental, and it partly MANUFACTURED the low flag groundedness we had pre-committed to reading as a property of practitioner notebooks — we pointed the problem-critique stage at a corpus we had instructed not to critique problems. Keeping `competition_metadata` does not substitute, since scoping documents are routed to parse only. **Stage-shaped categories REJECTED**: naming assumption/ambiguity/risk/mitigation buckets would make every document category-complete, and directed retrieval only buys something when a flat draw MISSES material — uniformly complete documents would move C2's mechanism up-funnel into a corpus B draws from equally, shrinking the contrast under test. The rule is document-natural and variance-preserving, not category-complete. The category enumeration is likewise replaced by a criterion ("a reader should come away knowing things they could not have guessed from the problem statement alone"), forcing concreteness without handing the model a template to normalize against; the risk is regression toward pre-2026-08-03 vagueness, and validation-strategy reporting is the specific thing to check. If specificity drops, restore a trimmed enumeration — do not revert the problem/rationale change, which is separable and independently motivated. Sequencing: unlike corpus topology or stage routing (pipeline-side, revisitable), the prompt is baked into every generated summary, so it had to land BEFORE the corpus expansion or be paid for twice | ingest/ingest_summaries.py::SUMMARY_SYSTEM |
| 2026-08-17 | Topical/domain matching in retrieval ACCEPTED as production-representative rather than engineered away, and the 2026-08-11 characterization is reinterpreted PRE-RUN accordingly. Same-domain/different-problem is a standard RAG failure mode and is where uncritical adoption — the AssistedDS failure this design targets — actually bites; a corpus that only ever returned structurally-matched documents would be performing the relevance filtering that C2's RETRIEVAL_STANCE exists to test, leaving the stance nothing to do. The 2026-08-11 cross-domain structure-matching result therefore stops being read as a well-behavedness claim and becomes a descriptive property of a corpus that no longer prioritizes it; the audit reports the domain-vs-structure mix either way, so the writeup can state what each condition reasoned over. Logged before eval runs because re-reading a recorded result after seeing new ones is what pre-registration forbids. Consequence for B1: with no LLM pass it cannot filter topically-plausible-but-wrong material, so part of any C2-vs-B1 gap measures filtering rather than structure — B2 remains the powered contrast | RESEARCH_DESIGN.md threats to validity; analysis/retrieval_audit.py |
| 2026-08-17 | `competition_metadata` RETAINED as a second document class; dropping it to a single corpus was considered and rejected. Production analogue: problem-scoping artifacts (project briefs, scoping docs, model-card intended-use, ML intake tickets) against the notebooks' solution artifacts — both classes exist in institutional knowledge bases, so a heterogeneous corpus is more faithful to the generalization target than a single-class one. Simplification would not have bought integrity: the class is retrieved identically by every condition (equal counts, same query), making it a shared constant rather than a confound, and separate indices are themselves a CONTROL — one index would let ranking dynamics between document classes vary across conditions, converting part of the measured effect into a retrieval-ranking artifact. Scoping documents are also the only source of problems AS POSED, gaps intact; a summary describes a problem as solved by someone who already resolved the ambiguity, so it cannot supply the reference distribution absence-detection needs. Tiebreaker on an untested component: keeping preserves the option to ablate in v1.5, dropping forecloses it. Routing scoping documents to parse rather than flag is acknowledged as historical (it predates the retirement of two-level retrieval, which needed a competition shortlist) and is now a roadmapped ablation rather than a claimed design | RESEARCH_DESIGN.md corpus construction + roadmap; pipeline/condition_b.py::_flat_retrieve; pipeline/nodes.py::parse_problem |
| 2026-08-17 | Summary prompt VALIDATED, closing the pending gate on the entry above. Old-vs-new on 35 notebooks across 8 competitions found the predicted specificity regression — naming a resampling scheme fell 69% to 49% once the enumeration became a criterion — and the pre-committed response was applied: restore a trimmed clause naming validation scheme and target metric, rather than revert the problem-framing and rationale change, which is separable and independently motivated. After restoration, summaries naming both scheme and metric rose above the old prompt (37% to 51%), with metric alone 57% to 77% and rationale +9pp at flat length. Tuning stopped there deliberately: further movement would fit the prompt to the probes rather than to summary quality. Two limits recorded with the result — the probes detect whether a summary names a validation scheme, not whether it named the right one, and the problem_framing probe was already saturated at 97% on the old summaries, so the qualitative dump rather than that probe is the evidence that problem framing changed. Rejected separately: a named "key learnings" section. Code4ML holds code cells rather than narrative write-ups, so learnings would be generated rather than extracted, colliding with the instruction not to infer unexpressed reasoning; it is also the closest thing to a stage-shaped bucket, mapping onto the advise stage and pre-digesting it for B | analysis/validate_summary_prompt.py; results/summary_prompt_validation.json |
| 2026-08-17 | Corpus composition accepted as-is ahead of the expansion: no substance filter beyond `kaggle_score > 0`, and the resulting quality spread kept deliberately. A 20-competition sample outside Lite-22 showed roughly a quarter are coursework rather than competitive practitioner work, a fact not visible from the Lite-22 slice; it does not change the 2026-08-11 position, since corpus noise is production-representative AND is where C's advantage over B should be measurable. Recorded alongside it is the opposite and larger deviation, corpus HOMOGENEITY: one model under one prompt gives every document a shared register and form that no real knowledge base has, measurable as a compressed similarity range (summaries fall off 0.008-0.016 rank 1 to 5 against 0.005-0.089 for human-written scoping documents). Logged as structural rather than POC-scale, and as biasing toward the null — uniformly well-organized documents are easiest for the condition reasoning over them flat, so structure should help least here and a measured C advantage is conservative with respect to it. Rejected as a fix: deliberately varying the summarizer, which would manufacture artificial variance and add a generation confound rather than reproduce real heterogeneity | RESEARCH_DESIGN.md threats to validity; analysis/review_summaries.py --unseen |
| 2026-08-17 | Runs isolated by experiment stage — a registry and artifact tree per stage, not a `stage` column — because `run_key` carries no corpus identifier and git tracks files rather than rows. Testing runs archived, never pooled with eval results. Policy in docs/DATA.md | docs/DATA.md; harness/registry.py; analysis/artifacts.py |
| 2026-08-17 | Corpus preserved as a deterministic export plus a tracked SHA-256 manifest: LLM summarization makes it irreproducible from code and sources, so results cite a hash. The vector store is a derived index, not an artifact | docs/DATA.md; ingest/export_corpus.py |
| 2026-08-17 | Retrieval re-characterized on the expanded corpus, QUALIFYING the 2026-08-11 finding that C's staged retrieval is broader than B's flat pass. It is not uniform: where B collapses, C is far broader (nomad2018 B draws 24/24 from one competition, C 33-38% concentration over 6-7), but on dogs-vs-cats C is MORE concentrated than B (67-71% vs 46%). C exceeds B on distinct techniques in all three competitions (19/15, 10/9, 11/9), which is the claim that survives. Run at both parse models: B is identical across them, confirming its flat retrieval is model-independent, and C varies only slightly, so the characterization is not an artifact of the dev model. `SIMILARITY_THRESHOLD` stays None — per-competition medians still span ~0.47-0.59, wider than the spread that decided it on 2026-08-11. Coursework competitions surface in C's vision retrieval, the first sign of the corpus composition skew reaching retrieved sets | results/retrieval_diversity_{haiku,sonnet}.json; results/retrieval_audit_{haiku,sonnet}.md |
| 2026-08-17 | `SIMILARITY_THRESHOLD` settled at `None`; the sweep is retired rather than re-run, since per-competition spread widened to ~0.47-0.59 on the rebuilt corpus. `analysis/calibration.py` kept for the record, out of the eval sequence | RESEARCH_DESIGN.md budget/attention confounds |
| 2026-08-21 | Judge validation given EVIDENCE PARITY by construction: one bounded bundle — submitted solution plus its ancestor chain in AIDE's search tree — built once and passed to both the LLM judge and the human reviewer. Rejected: excerpting logs by what the judge quoted, which is empty exactly when the judge saw nothing, so the reviewer could verify evidence the judge found but never discover evidence it missed, inflating agreement instead of measuring it. Bounding is forced, not chosen: AIDE runs at 500 steps. Consequence recorded — discarded branches are invisible to both raters. The reviewer is also held to the rubric's quote requirement, enforced when labels are read rather than only in the UI, since the judge's `acted_on_*` labels already survive that check and a weaker human standard would bias agreement on the class H2 depends on | docs/JUDGE_VALIDATION.md; analysis/judge_run.py |
| 2026-08-21 | Human anchor drawn ACROSS competitions by default, with a single-competition draw as the fallback rather than the plan: broader coverage says more about the judge, and narrowing is a concession to reviewer cost (dominated by problem context — 24 items from 24 competitions means learning 24 problems). Scope is chosen after the eval grid completes, when every run's bundle has been measured; the smoke cannot inform it, since only one competition exists at that point. Triggers fixed in advance and keyed on measured artifact size: narrow to one competition if multi-problem bundles are unreviewable or cannot populate all three classes, narrow further if even one competition's are. A low kappa is explicitly not a trigger, and a post-labeling rescope reports both draws | docs/JUDGE_VALIDATION.md |
| 2026-08-21 | Review artifact is a generated self-contained HTML page rather than markdown, on the reasoning that a labeling error is indistinguishable from judge disagreement in the final kappa, so layout is a validity concern and not ergonomics. Blinding became structural — the judge's label, quote, and reasoning are absent from the file — and item order is shuffled under a fixed seed, closing a leak where the stratified sample's (category, classification) ordering made position predictive of the label. Rejected: Argilla or Label Studio, disproportionate infrastructure for ~24 items; a CDN highlighter, which would break the open-from-disk requirement | analysis/judge_review_page.py |
| 2026-08-21 | Rubric freeze mechanically enforced by a SHA-256 pin test, closing the deferral recorded 2026-07-19 ahead of eval runs. An edit now fails the suite rather than silently invalidating every judgment made under the previous text; a legitimate amendment updates the pin in the same commit as a dated rubric entry and requires re-judging | tests/test_judge_rubric_pin.py |
| 2026-08-24 | Registry extended to the measures the analysis plan named but never recorded, and elapsed measures re-anchored to the first LLM call: the old anchor reported 0.0 for any run whose first draft worked | RESEARCH_DESIGN.md H1 higher-resolution + H3 timing anchor; harness/advance.py `AGENT_METRIC_FIELDS` |
| 2026-08-25 | Judge rubric revised three classes → two before any eval run (a revision, not an amendment; only the dev smoke had been judged, and it was re-judged). `acted_on_positive` was unreachable by construction, so contribution moves to post-hoc computation | docs/JUDGE_RUBRIC.md revision note; RESEARCH_DESIGN.md "Why contribution is computed rather than judged" |
| 2026-08-25 | Evidence bundle cut from the best node's ancestor chain to the best node alone, so evidence volume stops tracking search-tree shape. Rejected: widening to siblings, which answers a different causal question than H2 asks | analysis/judge_run.py "Why the bundle is one node, not a lineage" |
| 2026-08-25 | Base-rate counterfactual pre-registered for H2 — C2 flags judged a second time against the paired control solution, condition-blind. B2 confirmatory, B1/C1 descriptive. No new agent runs | RESEARCH_DESIGN.md H2 base-rate counterfactual |
| 2026-08-25 | Condition A promoted from contingent arm to primary, reversing 2026-07-16. Both causes follow from the same-day decision to cap runs below MLE-bench's 24h reference: the arm was priced at ~30 runs / +$200–250 under that reference and is ~2 days serial and ~$100 at a 4h cap, so it stopped trading against competition count (reducing n was the rejected alternative, on the grounds that this grid is already too small to absorb it); and the old trigger — "any condition lands below the plausible no-assistance range" — became circular once the published baseline was withdrawn from citation, since A is the only source for that range | RESEARCH_DESIGN.md Condition A note; docs/COST_ESTIMATE.md run matrix |
| 2026-08-25 | Published MLE-bench AIDE baseline withdrawn from citation entirely, tightening the 2026-07-16 position that allowed it "as context only". It carries two independent mismatches now — a different code model, and MLE-bench's reference budget against our reduced one — so neither a statistical nor a directional reading is admissible | RESEARCH_DESIGN.md Condition A note |
| 2026-08-27 | Development holdout stated as a design control: every competition used to develop or tune anything is ineligible for the eval subset. Reserves `random-acts-of-pizza` (integration) and `uw-madison-gi-tract-image-segmentation` (budget calibration). Generalizes an operational note that previously covered only smoke runs | RESEARCH_DESIGN.md experimental design controls |
| 2026-08-27 | Budget calibration pinned to `uw-madison-gi-tract-image-segmentation`, selected by leave-one-out retrieval probe of every vision competition in mle-bench's medium split. `cassava-leaf-disease-classification` was the best compute-regime match and is REJECTED: its top-15 is 15/15 from `plant-pathology-2020-fgvc7`, an eval competition, symmetrically. `tgs-salt-identification-challenge` kept as fallback but its steps are too cheap to reach the resource limits the eval tasks impose | RESEARCH_DESIGN.md Condition A → "Where the budget is calibrated" |
| 2026-08-27 | Retrieval concentration pre-registered as a descriptive covariate, before eval runs. Derived from existing `retrievals.json` fields — no instrumentation, no re-run; the decision is committing to it in advance rather than finding it afterward | RESEARCH_DESIGN.md analysis plan → covariate |
| 2026-08-31 | H1's three-part separation criterion demoted from pass/fail adjudication to a directional signal, pre-run: the pre-commitment stays, the verdict goes. Part of a wider trim of clinical-research register that is not ML convention, alongside dropping formal ATE/CATE notation from the H2 comparison and rejecting a proposed confirmatory/descriptive/exploratory tier section as bloat | RESEARCH_DESIGN.md analysis plan; analysis/stats.py `positive_signal` |
| 2026-08-31 | Missing-data rule pre-registered for runs producing no gradeable submission, closing an unregistered choice `analysis/stats.py` was already making silently. Agent failures floored to non-medal; environment failures repaired and re-run instead, which commits image composition to being frozen before eval runs begin | RESEARCH_DESIGN.md analysis plan; analysis/stats.py `NONSUBMISSION_FLOOR` |
| 2026-09-01 | Heavy dependencies ON, reversing the 2026-07-22 default: the build does not fail, and the lean image left AIDE's prompt asserting packages that were absent. Two environment repairs pinned in our requirements rather than mle-bench's, so the checkout stays identical to the pinned commit | scripts/setup_cloudbox.sh; cloudbox/agents/aide-prelude/requirements.txt |
| 2026-09-01 | aideml's "prefer PyTorch over TensorFlow" prompt line left UNMODIFIED despite the corpus running ~77% TF/Keras. It is upstream's, so editing it would tune the scaffold toward the treatment; recorded as a threat biasing toward the null instead | RESEARCH_DESIGN.md threats to validity |
